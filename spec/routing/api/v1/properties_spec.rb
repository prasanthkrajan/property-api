require "rails_helper"

RSpec.describe API::V1::Properties, type: :routing do
  # base_folder = 'api/v1'

  # it 'routes to index when hits GET /api/v1/properties' do
  #   expect(get("/#{base_folder}/properties")).to route_to({controller: "#{base_folder}/properties", action: 'index'})
  # end

  # it 'routes to create when hits POST /api/v1/properties' do
  #   expect(post("/#{base_folder}/properties")).to route_to({controller: "#{base_folder}/properties", action: 'create'})
  # end

  # it 'routes to update when hits PUT /api/v1/properties/1' do
  #   expect(put("/#{base_folder}/properties/1")).to route_to({controller: "#{base_folder}/properties", action: 'update', id: '1'})
  # end

  # it 'routes to delete when hits DELETE /api/v1/properties/1' do
  #   expect(delete("/#{base_folder}/properties/1")).to route_to({controller: "#{base_folder}/properties", action: 'destroy', id: '1'})
  # end

  # it 'routes to show when hits GET /api/v1/properties/1' do
  #   expect(get("/#{base_folder}/properties/1")).to route_to({controller: "#{base_folder}/properties", action: 'show', id: '1'})
  # end
end